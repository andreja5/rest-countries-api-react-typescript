# SearchInput Component

It is used to render input field.

## Code example
How to use SearchInput component:
```
<SearchInput 
  placeholder='Search for a country...'
  value={search}
  name='search'
  onSearch={handleFilter}
/>
```